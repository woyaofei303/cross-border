@AGENTS.md

## Agent skills

### Issue tracker（任务管理）

当前项目使用本地 Markdown 文件管理需求和任务，目录约定为 `.scratch/<feature>/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels（任务标签）

当前项目使用 Matt Pocock skills 默认的任务状态标签。详见 `docs/agents/triage-labels.md`。

### Domain docs（领域文档）

当前项目是单上下文项目：根目录 `CONTEXT.md` 记录领域语言，`docs/adr/` 记录架构决策。详见 `docs/agents/domain.md`。

### Skill usage

Matt Pocock skills 的安装命令、slash 命令和当前项目工作流记录在 `docs/agents/skills-usage.md`。
