# Triage labels（任务标签）

当前项目使用 Matt Pocock skills 默认的任务状态标签。

- `needs-triage`：等待维护者评估
- `needs-info`：等待需求提出者补充信息
- `ready-for-agent`：需求已经明确，可以交给 agent 独立处理
- `ready-for-human`：需要人类开发者处理
- `wontfix`：确认不处理

本地 Markdown issue 中用下面的格式记录标签：

```text
Triage label: ready-for-agent
```
